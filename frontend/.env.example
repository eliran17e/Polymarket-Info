# Production only. Set to the deployed backend's URL (no trailing slash), e.g.
#   VITE_API_BASE=https://polymarket-info-api.onrender.com
# Leave UNSET for local dev — the Vite proxy handles /api → backend automatically.
VITE_API_BASE=
